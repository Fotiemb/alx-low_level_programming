#include <stdio.h>
#include "main.h"
/*
 * mul - function that multiplies two integers
 * Return: mult it's the result
 */
/*mul function*/
int mul(int a, int b)
{
	int mult;

	mult = (a * b);

	return (mult);
}
