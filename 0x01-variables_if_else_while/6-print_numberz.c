#include <stdio.h>

/**
 * main- this is the entry point
 * Return: Always return 0 (Success0
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9 ; n++)
	{
		putchar('0' + n);
	}

	putchar('\n');

	return (0);
}
